<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Categorías</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>
<body>
    <div class="contenedor">
        <div class="aside">
            <h2><a href="/">Administrador</a></h2>
            <hr>
            <ul class="categories">
                <li><a href="/categorias">Ver Categorias</a></li>
                <li><a href="/archivos">Ver Archivos</a></li>
                <li><a href="#">Ver mas...</a></li>
            </ul>
        </div>
        <section>
            <div class="header">
                <h4>Adminsitrador de datos</h4>
            </div>
            <hr>
            <div class="main">
                <div class="title">
                    <h3>Crear Categoría</h3>
                </div>
                <div class="data">
                    <form action="/categorias/<%= category.id %>?_method=PUT" method="POST">
                        <div class="form-group">
                            <label for="title">Nombre de la categoría</label>
                            <input type="text" id="title" class="form-control" name="title" value="<%= category.title %>">
                        </div>
                        <div class="form-group">
                            <label for="permission">Permisos</label>
                            <select name="permission" id="permission" class="form-control">
                                <option value="teacher" <%= category.permission == "teacher" ? "selected"  : null %>>Profesor</option>
                                <option value="student" <%= category.permission == "student" ? "selected"  : null %>>Alumno</option>
                                <option value="parent" <%= category.permission == "parent" ? "selected"  : null %>>Padre</option>
                            </select>
                        </div>
                        <button class="btn btn-primary">Guardar Categoría</button>
                    </form>
                </div>
            </div>
        </section>
    </div>
    <div class="footer">
        <p>Footer</p>
      </div>
</body>
</html>